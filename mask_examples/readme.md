These are numpy array files.  They are some example numpy 3D arrays from the PARADIM Mask RCNN examples.  They were created with:

```np.save('data.npy', num_arr)```

and can be loaded back into a notebook or script with 

```new_num_arr = np.load('data.npy')```